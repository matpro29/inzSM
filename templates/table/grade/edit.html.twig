{% extends 'base.html.twig' %}

{% block title %}Edytuj ocenę{% endblock %}

{% block body %}
    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    </style>

    <div class="example-wrapper">
        <h1>Edytuj ocenę ✅</h1>

        {{ include('table/grade/form/_new_form.html.twig', {'button_label': 'Zapisz zmiany'}) }}

        <a href="{{ path('grade_index') }}">Wróć</a>

        {{ include('table/grade/form/_delete_form.html.twig', {'button_label': 'Usuń ocenę'}) }}
    </div>
{% endblock %}
