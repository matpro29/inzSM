<form method="post" action="{{ path('user_course_delete', {'id': user_course.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user_course.id) }}">
    <button class="btn">Usuń</button>
</form>