{% extends 'base.html.twig' %}

{% block title %}Nowa ocena{% endblock %}

{% block body %}
    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    </style>

    <div class="example-wrapper">
        <h1>Nowa ocena ✅</h1>

        {{ include('course/grade/form/_new_form.html.twig') }}
        </br>
        <a href="{{ path('course_user_grade', {'courseId': course.id, 'userId': userInfo.id}) }}">Wróć</a>
    </div>
{% endblock %}

