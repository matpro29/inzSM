{% extends 'base.html.twig' %}

{% block title %}Edytuj ocenę końcową{% endblock %}

{% block body %}
    <div class="container">
        <h1>Edytuj ocenę końcową</h1>

        {{ include('course/grade/form/_end_form.html.twig') }}
        </br>
        </br>
        <a href="{{ path('course_user_grade', {'courseId': course.id, 'userId': userInfo.id}) }}" class="back">{% trans %}Wróć{% endtrans %}</a>
    </div>
{% endblock %}

