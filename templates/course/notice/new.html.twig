{% extends 'base.html.twig' %}

{% block title %}Nowe powiadomienie{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('build/js/flatpickr.css') }}">
{% endblock %}

{% block body %}
    <div class="container">
        <h1>{% trans %}Nowe powiadomienie{% endtrans %}</h1>

        {{ include('course/notice/form/_new_form.html.twig') }}
        </br>
        <a href="{{ path('course_show', {'id': course.id}) }}" class="back">{% trans %}Wróć{% endtrans %}</a>
    </div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script src="{{ asset('build/js/flatpickr.js') }}"></script>
{% endblock %}