{% extends 'base.html.twig' %}

{% block title %}Nowy webinar{% endblock %}

{% block body %}
    <div class="container">
        <h1>Nowy webinar</h1>

        {{ include('course/webinar/form/_new_form.html.twig') }}
        </br>
        <a href="{{ path('course_show', {'id': course.id}) }}" class="back">{% trans %}Wróć{% endtrans %}</a>
    </div>
{% endblock %}